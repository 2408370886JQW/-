# 【原型版本更新说明】

**产品名称：** FIND ME 发现我

**更新日期：** 2026年3月2日

**更新范围：** 相见模块（商家列表 / 套餐详情 / 订单管理 / 评价体系）

---

## 一、商家列表模块

**新增：** 商家卡片列表增加好评率标签展示。好评率 95% 以上显示绿色标签，90% 以上显示蓝色标签，用户在浏览列表阶段即可识别高口碑商家。场景推荐列表与纯团购列表两处同步生效。

**新增：** 商家列表顶部增加排序栏，支持四种排序维度切换——智能推荐（默认）、好评优先、离我最近、价格最低。排序栏采用胶囊按钮样式，选中态深色高亮，场景推荐列表与纯团购列表两处均适用。

**新增：** 商家卡片价格信息左侧增加「人均消费」标签，展示人均消费区间（如 ¥120-180），帮助用户在浏览阶段快速判断消费水平。场景推荐列表、纯团购列表、商家详情页三处同步展示。

**新增：** 距离排序模式下增加三重视觉指示器——卡片左侧渐变色条（绿→黄→橙，距离越近越绿）、图片左上角排名徽章（#1→#4）、以及醒目的距离高亮标签（绿色"距你很近" / 黄色"距你适中" / 橙色"距你较远"）。切换回其他排序模式时指示器自动隐藏。

**修复：** 距离排序模式下排名徽章（#1-#4）遮挡店名的定位问题。将排名徽章从店名区域移至图片容器左上角，同时调整运营标签（限时特惠/新店开业）和匹配度标签在距离排序模式下的偏移位置，三层标签垂直排列互不遮挡。

---

## 二、套餐对比模块（新增）

**新增：** 套餐对比功能。在商家详情页的每个套餐卡片左上角增加 VS 对比勾选按钮，用户可选择 2-4 个套餐加入对比。

**新增：** 对比浮层。选中套餐后页面底部弹出对比栏，展示已选套餐缩略图、已选数量及「开始对比」按钮。

**新增：** 对比详情页。全屏弹窗以横向表格形式展示对比结果，覆盖五个维度：团购价、折扣力度、套餐内容、有效期、使用规则。底部提供「清空对比」和「继续选购」操作。

**新增：** 对比结果分享功能。对比详情页底部增加渐变色「分享对比结果给朋友」按钮，点击后弹出分享弹窗，包含套餐对比摘要卡片（各套餐名称、商家、价格、折扣及最低价推荐），提供微信、朋友圈、复制文字、短信四种分享方式。复制文字功能自动生成结构化对比文本摘要。

---

## 三、订单管理模块（新增）

**新增：** 「我的」页面增加购买记录入口，入口位于「我的预约」模块下方。

**新增：** 购买记录页面，包含以下子功能：

| 功能点 | 说明 |
|--------|------|
| 消费概览卡片 | 页面顶部展示总订单数、累计消费金额、累计节省金额三项统计 |
| 状态筛选标签 | 支持按「全部 / 待使用 / 已使用 / 已过期」四种状态筛选 |
| 订单卡片列表 | 每张卡片展示商家图片、套餐名称、价格、场景标签、有效期倒计时 |
| 订单详情页 | 展示核销码、商家信息、套餐内容明细、订单信息、联系商家与申请退款按钮 |

---

## 四、评价体系模块（新增）

**新增：** 已使用订单卡片底部增加橙色渐变「去评价」按钮，仅对已使用且未评价的订单显示。

**新增：** 评价弹窗组件，包含以下交互元素：

| 交互元素 | 说明 |
|----------|------|
| 星级评分 | 1-5 星，对应非常不满意→非常满意五个等级 |
| 快捷标签 | 12 个预设标签（如味道很好、性价比高、环境优雅等），最多选 5 个 |
| 文字评价 | 自由输入框，200 字限制 |

**新增：** 评价提交后订单卡片状态更新为「已评价」，卡片底部显示用户评分星级。订单详情页底部展示完整评价内容（星级 + 标签 + 文字 + 评价时间）。

---

## 五、商家端相关

**新增：** 商家回复评价功能。在订单详情页的用户评价区域下方增加商家回复展示卡片，采用蓝色背景与用户评价的白色背景形成视觉区分，包含商家图标、商家名称、认证徽章、回复内容及回复时间。

**新增：** 订单卡片底部对已评价且有商家回复的订单显示「商家已回复」蓝色标签。

**补充说明：** 当前版本商家回复为模拟数据展示，用户提交评价后 3 秒自动生成模拟商家回复，用于演示完整的用户-商家互动链路。后续接入真实商家后台后替换为实际回复数据。

---

## 【本次更新重点】

1. **商家筛选排序体系上线：** 新增四维排序（智能推荐 / 好评优先 / 离我最近 / 价格最低）+ 好评率标签 + 人均消费标签 + 距离排序视觉指示器，构成完整的商家筛选决策链路，显著降低用户选择成本。

2. **套餐对比功能闭环：** 从勾选对比→横向表格对比→分享对比结果，形成完整的套餐决策辅助工具链，支持用户独立决策或邀请朋友共同决策。

3. **订单全生命周期管理：** 新增购买记录页 + 订单详情 + 评价系统 + 商家回复，覆盖「购买→使用→评价→互动」完整闭环，为后续接入真实交易和商家后台奠定原型基础。

4. **需设计/开发关注：** 距离排序指示器涉及三层标签（排名徽章 / 运营标签 / 匹配度标签）的动态偏移逻辑，开发时注意各标签在不同排序模式下的显隐和定位规则。
