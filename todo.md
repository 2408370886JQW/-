# Todo List

## Phase 15: 修复相见页显示与适配问题
- [ ] 修复左侧分类导航二级标题（小字）显示问题
  - [ ] 检查 `text-[10px]` 和颜色样式是否被覆盖
  - [ ] 确保容器高度足够容纳两行文字
- [ ] 修复移动端适配变形问题
  - [ ] 调整左侧 Sidebar 宽度（建议固定为 `w-20` 或 `w-22`，避免过宽）
  - [ ] 检查右侧内容区域的 `flex-1` 和 `min-w-0` 设置，防止内容溢出
  - [ ] 优化顶部搜索栏在小屏幕下的布局（减少 padding 或图标大小）
  - [ ] 检查商品卡片在窄屏下的显示（图片比例、文字截断）
- [ ] 再次验证全交互逻辑是否受影响
